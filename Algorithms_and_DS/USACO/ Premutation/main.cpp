#include <bits/stdc++.h>

int main()
{
  int t; std::cin >> t;
  while(t--)
  {
    int n; std::cin >> n;
    std::vector<std::vector<int>> vv(n);
    for(int i = 0; i < n; ++i) {int val; std::cin >> val;}
    for(int i = 0; i < n; ++i)
    {
      for(int j = 0; j < n; ++j)
      {

      }
    }

  }
}
